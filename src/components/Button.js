import React from "react";

export const Button = () => {
  return (
    <div>
      <button type="submit" class="signupbtn">
        Sign in
      </button>
    </div>
  );
};

export default Button;
